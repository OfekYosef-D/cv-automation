---
description: React and Next.js patterns
globs: apps/web/**/*.{ts,tsx}
alwaysApply: false
---

# React & Next.js

- Use functional components and hooks.
- Server Components by default; add `"use client"` only when needed (state, events).
- Colocate components with their routes when possible.
- Use `layout.tsx` for shared layout, `page.tsx` for route content.
